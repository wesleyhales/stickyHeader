##init

```javascript
jQuery("#table_id").stickyHeader(
           {            
                topmenu : {
                    parentFormID : '#results_form',
                    addToTopMenu : ['.manage_buttons']
                }
            }
 );
```
